const renderForms = (forms, registerRef) => {
  const returnArray = [];

  for (const field of forms) {
    switch (field.type) {
      case "password":
        break;

      default:
        break;
    }
  }
};

export { renderForms };
